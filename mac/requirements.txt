# Mac MLX model serving requirements
# Install with: pip install -r requirements.txt
# Or run: ./setup.sh (handles venv, deps, and fluidaudiocli)

# Core MLX
mlx>=0.22.0
mlx-lm>=0.31.2  # 0.31.2+ required for gemma4 support
mlx-vlm>=0.4.4  # 0.4.4+ required for gemma4 support

# LLM server proxy
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# TTS — Chatterbox Turbo via mlx-audio
mlx-audio

# STT — Parakeet TDT via fluidaudiocli (Neural Engine)
# No pip dependency — uses fluidaudiocli binary (built by setup.sh)
# See: https://github.com/FluidInference/FluidAudio

# Audio processing
soundfile>=0.12.1
numpy>=1.26.0
