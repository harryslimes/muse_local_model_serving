# Mac MLX model serving requirements
# Install with: pip install -r requirements.txt

# Core MLX
mlx>=0.22.0
mlx-lm>=0.21.0

# LLM server proxy
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# TTS — Chatterbox Turbo MLX
chatterbox-audio>=0.2.0

# STT — Parakeet TDT MLX
parakeet-mlx>=0.1.0

# Audio processing
soundfile>=0.12.1
numpy>=1.26.0
